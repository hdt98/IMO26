Title: IMO 2026 P1 - direct long-request workflow

Solve IMO 2026 Problem 1 using the durable direct background workflow
documented at workflows/codex.md in this repo.

Authoritative problem ID: imo2026_p1
Authoritative problem file: problems/imo2026_p1.txt

Read the workflow contract completely before acting and follow it exactly.

Credentials — choose one endpoint and its matching token:
  Cloud endpoint (from Codex config):
    - API URL: ~/.codex/config.toml [model_providers.custom] base_url
      + /chat/completions
    - Token: ~/.codex/auth.json key "OPENAI_API_KEY"
    - Model: ~/.codex/config.toml top-level "model"
  Direct IP endpoint:
    - API URL: http://165.245.166.41:30000/v1/chat/completions
    - Token: onenx-dev-JgZ0YeSTHeTVh057uomgjF02
    - Model: GLM-5.2-FP8
Never print or persist the token value.

Always pass --api-url, --api-key, and --model explicitly as command-line
arguments. Launch the orchestrator directly via exec_command (NOT inside
a screen session). The process becomes a child of the Codex app-server
and is automatically cleaned up when the goal is stopped — no manual
process cleanup is needed.

After launch, wait 30 seconds and check these files in the run directory:
  1. cat <run-dir>/progress.log — one line per state transition or error
  2. tail <run-dir>/stderr.log — API call failures and tracebacks
If progress.log shows any ERROR line, the run is failing. Do not keep
waiting; diagnose the error and fix it before retrying. Do NOT poll for
sentinel files — the orchestrator never creates them.

Monitor the orchestrator using write_stdin with the session_id from launch
(yield_time_ms=300000 for 5-minute checks) and exec_command to check
progress.log and state.json. If write_stdin returns an exit_code, the
orchestrator has finished — check results. Only report when an iteration
completes or an error occurs.

This must be a fresh, independent run. Do not read prior solutions, run
logs, verification reports, scratch work, or artifacts. The Desktop parent
is an orchestrator only and may not solve or grade the mathematics. Keep
the Goal active until the background workflow either persists a candidate
that earns five consecutive independent passes or records a legitimate
terminal failure.

Do not interpret a UI wait timeout as a stream failure, do not silently
duplicate active requests (this means YOUR session's own requests only —
another session running the same problem is not duplication), and do not
lower the acceptance threshold.

