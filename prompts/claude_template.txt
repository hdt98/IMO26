Title: IMO 2026 P{n} - direct long-request workflow

Solve IMO 2026 Problem {n} using the durable direct background workflow
documented at workflows/claude.md in this repo.

Authoritative problem ID: imo2026_p{n}
Authoritative problem file: problems/imo2026_p{n}.txt

Read the workflow contract completely before acting and follow it exactly.

Credentials - Direct IP endpoint:
  - API URL: http://165.245.166.41:30000/v1/chat/completions
  - Token: onenx-dev-JgZ0YeSTHeTVh057uomgjF02
  - Model: GLM-5.2-FP8
Never print or persist the token value.

Formal verification policy for this run:
  - Keep the formal gate required with `--lean-mode required`; local Lean
    runs first.
  - Enable AXLE only as fallback with `--axle-mode fallback`.
  - Use the matching hosted environment with
    `--axle-environment lean-4.28.0`.
  - Inject `AXLE_API_KEY` through the process environment. Never put its
    value in the prompt, command line, logs, or artifacts.
If `AXLE_API_KEY` or the AXLE client is unavailable, stop and report that
before launch; do not silently disable AXLE or weaken the formal gate.

Launch the orchestrator using the Bash tool with run_in_background: true
(NOT nohup, NOT screen). This gives Claude Code a backgroundTaskId for
tracking. The process survives session stops by design, which is desired
for long-running orchestrator runs. Always pass --api-url, --api-key, and
--model explicitly as command-line arguments.

After launching, wait 30 seconds then check these files
in the run directory:
  1. cat <run-dir>/progress.log - one line per state transition or error
  2. tail <run-dir>/stderr.log - API call failures and tracebacks
If progress.log shows any ERROR line, the run is failing. Do not keep
waiting; diagnose the error and fix it before retrying. Do NOT poll for
sentinel files - the orchestrator never creates them.

Before each monitoring check, verify the orchestrator PID is still alive
(ps -p <pid>). If the PID is gone, RESTART the orchestrator immediately -
do NOT monitor or piggyback on another session's run.

When an iteration completes (VERIFY or CLASSIFY), read the latest artifacts
from the run_XX/ subdirectory in the run directory to report substantively:
  - cat <run-dir>/run_XX/classify_YY.md — the classification verdict (yes/improve/no)
  - head -30 <run-dir>/run_XX/verify_YY.md — key findings from the verification
  - cat the latest <run-dir>/run_XX/lean_verify_NN.txt — the formal gate,
    including local Lean and AXLE PASS/FAIL/SKIP status
Summarize what the verifier found (critical errors, justification gaps) and
what the classifier decided. State whether the formalization passed and whether
the verifier judged `imo_problem` faithful to the natural-language problem.
Do not just report token counts — explain what happened mathematically and what
the next step is trying to fix. Keep the summary concise but substantive.

The orchestrator has built-in protections that work without agent
intervention: a wall-clock timeout (threading.Timer, 5400s per API call) that
prevents hung processes, infrastructure error detection with exponential
backoff (waits instead of burning through runs when endpoint is down), a
lock file that prevents duplicate runs for the same problem, a three-tier
classifier (yes/improve/no) that distinguishes minor gaps from critical
errors, and a pivot mechanism (3 consecutive failures triggers a fresh
SOLVE with a different-approach hint).
Do not interfere with these mechanisms.

Note: state.json may show stale consecutive_passes values because it is
saved before the reset logic runs. The actual in-memory values are correct.

This must be a fresh, independent run. Do not read prior solutions, run
logs, verification reports, scratch work, or artifacts. The Desktop parent
is an orchestrator only and may not solve or grade the mathematics. Keep
the Goal active until the background workflow either persists a candidate
that earns five consecutive independent passes and passes the configured
formal-verification gate, or records a legitimate terminal failure.

After the goal is achieved or failed, clean up your own orchestrator
process (kill <pid>). Do NOT kill processes that belong to other sessions.
If the goal was stopped and resumed, check if the orchestrator process
is still alive - if alive, resume monitoring; if not, start a fresh run.

Do not interpret a UI wait timeout as a stream failure, do not silently
duplicate active requests (this means YOUR session's own requests only -
another session running the same problem is not duplication), and do not
lower the acceptance threshold.
